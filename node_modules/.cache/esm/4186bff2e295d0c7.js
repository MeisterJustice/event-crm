let crypto,cloudinary,cloudinaryAuth,cloudinaryStorage;_e29‍.w("crypto",[["default",["crypto"],function(v){crypto=v}]]);_e29‍.w("cloudinary",[["default",["cloudinary"],function(v){cloudinary=v}]]);_e29‍.w("../config/auth",[["default",["cloudinaryAuth"],function(v){cloudinaryAuth=v}]]);_e29‍.w("multer-storage-cloudinary",[["default",["cloudinaryStorage"],function(v){cloudinaryStorage=v}]]);



cloudinary.config({
	cloud_name: cloudinaryAuth.cloudinaryUpload.cloud_name,
	api_key: cloudinaryAuth.cloudinaryUpload.api_key,
	api_secret: cloudinaryAuth.cloudinaryUpload.api_secret
});

const storage = cloudinaryStorage({
  cloudinary: cloudinary,
  folder: 'event-crm',
  allowedFormats: ['jpeg', 'jpg', 'png'],
  filename: function (req, file, cb) {
  	let buf = crypto.randomBytes(16);
  	buf = buf.toString('hex');
  	let uniqFileName = file.originalname.replace(/\.jpeg|\.jpg|\.png/ig, '');
  	uniqFileName += buf;
    cb(undefined, uniqFileName );
  }
});

module.exports = {
	cloudinary,
	storage
}