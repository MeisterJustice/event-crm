let User,passport;_ec3‍.x([["getIndex",()=>getIndex],["getFacebookLogin",()=>getFacebookLogin],["postFacebookLogin",()=>postFacebookLogin],["getRegister",()=>getRegister],["postRegister",()=>postRegister],["getLogin",()=>getLogin],["postLogin",()=>postLogin],["getLogout",()=>getLogout]]);_ec3‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_ec3‍.w("passport",[["default",["passport"],function(v){passport=v}]]);


       const getIndex = (req, res, next) => {
  res.render('index');
}

       const getFacebookLogin = passport.authenticate('facebook', {
  scope: ['email']
});

       const postFacebookLogin = passport.authenticate('facebook', {
  successRedirect: '/',
  failureRedirect: '/login',
  session: false
})

       const getRegister = async (req, res, next) => {
  res.render("auth/register");
}

       const postRegister = async (req, res, next) => {
  let newUser = await new User({
    username: req.body.username,
    firstName: req.body.firstName,
    lastName: req.body.lastName,
    sex: req.body.sex,
    email: req.body.email
  });
  await User.register(newUser, req.body.password);
  // req.flash("success", "welcome");
  res.redirect("/event");
}

       const getLogin = async (req, res, next) => {
  res.render("auth/login");
}

       const postLogin = async (req, res, next) => passport.authenticate("local", {
  successRedirect: "/event",
  failureRedirect: "/login"
})(req, res, next)

       const getLogout = async (req, res, next) => {
  req.logout();
  // req.flash("success", "Logged you out!");
  res.redirect("/");
}